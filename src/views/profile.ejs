<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <%- include('partials/header', {userId, page:'profile'}) %>
        <main>
            <section class="data-container">
                <article class="user-img">
                    <img alt="Imagen de perfil" src="<%= userData.user.user_pfp %>">
                </article>
                <article class="user-data">
                    <span class="h1">
                        <%= userData.user.user_username %>
                    </span>
                    <br>
                    <span class="h2">
                        <%= userData.user.user_name %>
                        <%= userData.user.user_last_name %>
                    </span>
                </article>
            </section>

            <h1>Recetas de <%= userData.user.user_username %>
            </h1>

            <section class="recipes">
                <ul>
                    <% userData.recipes.forEach((recipe, index)=> { %>
                        <li>
                            <a href="/recipe/view/<%= recipe.recipe_id %>">
                                <img src="<%= recipe.recipe_img[0] %>" class="recipe"
                                    alt="Imagen de la receta <%= recipe.recipe_id %>">
                                <span class="recipe-h1"><%= recipe.recipe_name %></span>
                                <span class="recipe-h2"></span>
                            </a>
                        </li>
                        <% }) %>
                </ul>
            </section>
        </main>
</body>

</html>
<style>
    html {
        font-size: 16px;
    }

    * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
    }

    body {
        height: 100dvh;
        width: 100dvw;
        overflow-x: hidden;
    }

    main {
        width: 98%;
        min-height: 100%;
        margin: 0 auto;
        background: green;
    }

    .user-img {
        width: 300px;
        border-radius: 50%;
        background: #ccc;
        aspect-ratio: 1;
        display: flex;
        align-items: center;
        justify-content: center;
        overflow: hidden;
        border: 2px black solid;
    }

    article img {
        display: inline-block;
        width: 100%;
        height: 100%;
        aspect-ratio: 1;
        object-fit: contain;
    }

    .user-data {
        width: 96%;
        height: auto;
        background: blue;
        margin: 0.5rem auto;
        padding: 0.5rem;
    }

    section.recipes {
        width: 96%;
        min-height: 10rem;
        background: rgb(0, 255, 213);
        margin: 0.5rem auto;
        padding: 0.5rem;

        & a {
            display: flex;
            flex-direction: column;
            text-decoration: none;
        }
    }

    h1 {
        font-size: 2rem;
        padding-inline: 1rem;
    }

    a {
        & img.recipe {
        aspect-ratio: 16/9;
        width: 96%;
        object-fit: cover;
        object-position: center center;
        margin: 0 auto;
    }
    }

    .data-container {
        display: flex;
        gap: 0.5rem;
        padding: 0.5rem;
    }

    span.h1 {
        font-size: 2.25rem;
        font-weight: bold;
    }

    span.h2 {
        font-size: 1.25rem;
    }

    ul {
        list-style: none;
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
    }
</style>